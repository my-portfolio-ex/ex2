import React, { useRef } from 'react';

import * as S from './styled';

interface ProjectProps {
  num: number;
}

export const FirstProject: React.FC<ProjectProps> = ({ num }) => {
  return <section></section>;
};
